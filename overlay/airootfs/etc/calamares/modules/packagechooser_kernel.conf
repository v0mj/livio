---
mode: required
method: netinstall-select

labels:
  step: "Kernel"

default: kernel-livio

items:
  - id: kernel-livio
    name: "Livio Gaming Kernel"
    description: "Use linux-livio, a Livio-branded gaming kernel generated from Arch's maintained Zen kernel recipe. The LTS kernel stays installed as a fallback."
    screenshot: "/etc/calamares/images/kernel-livio.svg"
  - id: kernel-arch-stable
    name: "Arch Stable Kernel"
    description: "Use the normal Arch Linux kernel plus headers. Choose this if you prefer the most standard upstream Arch path."
    screenshot: "/etc/calamares/images/kernel-stable.svg"
  - id: kernel-lts-only
    name: "LTS Fallback Only"
    description: "Use only the long-term-support kernel that Livio installs as the safety net. Best for maximum conservative stability."
    screenshot: "/etc/calamares/images/kernel-lts.svg"
